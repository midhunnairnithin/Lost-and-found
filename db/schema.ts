import { integer, sqliteTable, text } from "drizzle-orm/sqlite-core";
export const itemReports=sqliteTable("item_reports",{id:integer("id").primaryKey({autoIncrement:true}),reference:text("reference").notNull(),title:text("title").notNull(),reportType:text("report_type").notNull(),category:text("category").notNull(),description:text("description").notNull(),location:text("location").notNull(),incidentDate:text("incident_date").notNull(),imageData:text("image_data"),imageAltText:text("image_alt_text"),privateVerificationDetail:text("private_verification_detail"),reporterContact:text("reporter_contact").notNull(),createdAt:text("created_at").notNull()});
export const itemClaims=sqliteTable("item_claims",{id:integer("id").primaryKey({autoIncrement:true}),itemId:integer("item_id").notNull(),claimantName:text("claimant_name").notNull(),claimantContact:text("claimant_contact").notNull(),ownershipDetails:text("ownership_details").notNull(),createdAt:text("created_at").notNull()});
